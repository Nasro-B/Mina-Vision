# Mina Vision — tâches restantes de réconciliation

> État au 2026-08-20 13:41 (Africa/Lagos). Ce tableau consolide les dernières preuves du [journal de réconciliation](2026-07-29-mina-reconciliation-log.md), de la [preuve de release](../../operations/RELEASE-EVIDENCE-2026-07.md), du [ledger du chat natif](2026-07-29-mina-native-chat-scope-ledger.md), de la gate Android/Node rejouée avant push et des sondes runtime non destructives du 2026-08-20. Il ne transforme aucune recette matérielle, compte externe ou décision produit en succès.

## Clos avec preuve récente

- [x] Validation actuelle : après les derniers changements Sandbox, `npm run verify:release` a terminé avec `status:"pass"` le `2026-08-20 12:01` : unité `442` fichiers, `3 564` tests passés et `6` ignorés en `289,23 s`; intégration `18/18` fichiers et `49/49` tests en `33,39 s`; smoke Electron `SMOKE OK`; smoke SQLite/Electron `{"ok":true,"electron":"43.1.0","abi":"148"}` ; `sandbox_smoke` `pass` avec `MINA_SANDBOX_OK v22.23.2`; `verify` code `0`. La gate Android locale précédente reste `.\gradlew.bat testDebugUnitTest lintDebug assembleDebug --no-daemon` `BUILD SUCCESSFUL`. Les gates manuelles du rapport release restent `unrun` pour Android physique, lumière Home, comptes fournisseurs et tour vocal local hors réseau ; `sandbox_isolation_acceptance` est maintenant `pass` avec `windows_sandbox_smoke_passed`.
- [x] Polish UI/UX des boutons et contrôles : les boutons d’action, boutons fantômes, actions mission, boutons d’accueil, boutons icônes rail/caméra et contrôles mémoire/sandbox/providers partagent maintenant des tokens visuels communs (`--button-radius`, `--button-shadow`, `--button-shadow-hover`, `--button-shadow-pressed`). Le `translateY(1px)` global a été supprimé pour éviter le micro-déplacement au clic, les contrôles secondaires ont un plancher tactile, et `prefers-reduced-motion` désactive désormais aussi les transitions. Le bouton mémoire `Initialiser` ne laisse plus l’utilisateur avec le code brut `keyring_already_initialized` quand le coffre existe déjà et est sain : il rafraîchit l’état puis indique de coller la phrase et cliquer `Déverrouiller`, sans lancer la réinitialisation de secours. Preuves : `npx vitest run tests/ui-polish-contract.test.mjs tests/accessibility-contract.test.mjs --maxWorkers=1 --no-file-parallelism` a passé `2/2` fichiers et `11/11` tests ; `npx vitest run tests/memory-initialized-ux-dom.test.mjs --maxWorkers=1 --no-file-parallelism` a d’abord échoué en rouge sur le log brut, puis passe `1/1`; `npx vitest run tests/memory-flow-dom.test.mjs tests/memory-repair-dom.test.mjs tests/welcome-flow-dom.test.mjs tests/accessibility-contract.test.mjs --maxWorkers=1 --no-file-parallelism` passe `4/4` fichiers et `14/14` tests ; `npm run test:smoke` a rendu `SMOKE OK`.
- [x] Firebase local : projet et app Android inventoriés en lecture seule ; règles owner/device testées dans Auth/Firestore/RTDB/Storage Emulator. Le `2026-08-09`, `npm run test:firebase:emulator` a terminé avec le code `0` : smoke Firestore/Storage `rules_enforced` et `network: loopback_only`, puis `7/7` tests de règles owner/device verts, dont les flux RTDB. Le diagnostic refuse aussi, avant signature, un compte de service d'un autre projet. Les règles Firestore/Storage testées ici ne sont pas déployées ; la publication RTDB est relatée séparément ci-dessous.
- [x] Firebase RTDB cloud : avec la CLI Firebase connectée à `mina.vision.ai@gmail.com`, `firebase deploy --only database --project mina-vision` a validé la syntaxe puis publié `database.rules.json` dans `mina-vision-default-rtdb`. Cette action ne déploie ni Firestore, ni Storage, ni Functions.
- [x] Firebase cloud : une clé de compte de service Mina Vision est configurée uniquement dans `env/` ignoré ; la release Storage active refuse l’anonyme. Une recette réelle avec UID temporaire a vérifié custom token, Firestore valide/refus invalide, Storage propriétaire/refus inter-propriétaire, puis sauvegarde chiffrée et restauration. La recette a supprimé ses objets et son utilisateur ; le listing Storage final est vide. La sonde locale reste volontairement `firebase_cloud_unverified`, car elle ne fait aucun appel cloud implicite.
- [x] LM Studio local : le `2026-08-09`, `lms server status` a confirmé le serveur sur le port `1234`, et `lms ps` a listé Gemma `google/gemma-4-e2b` ainsi que Nomic `text-embedding-nomic-embed-text-v1.5` chargés. L’adaptateur réellement consommé par Mina a exécuté une génération streamée locale : `MINA_LOCAL_RELOAD_OK`, `9` fragments, fin `stop`. La CLI affichait `CONTEXT 509` pour Gemma lors de ce contrôle ; ce nombre est rapporté tel quel, sans l’interpréter comme la limite de contexte théorique du modèle.
- [x] TTS locale hors réseau : Kokoro a effectué un warm-up puis une synthèse française PCM à `24 000 Hz` avec `MINA_OFFLINE=true`, sans microphone ni téléchargement.
- [x] Chargement STT local : Whisper `Xenova/whisper-small` a chargé et inféré en `q8` avec `local_files_only:true` via le vrai chargeur applicatif. Les binaires q4f16 ont été rejetés par le runtime CPU ; la variante q8 est donc le défaut testé. La configuration locale ignorée active désormais `MINA_STT_ENABLED=true` et `MINA_OFFLINE=true`. La preuve utilise un PCM silencieux : elle valide chargement/inférence, pas une transcription française au microphone.
- [x] Vision locale Mina : l’adaptateur réellement utilisé par Mina a analysé une image locale de contrôle avec Gemma à `4096` tokens et un flux. La réponse finale a été reçue séparément du raisonnement du modèle ; seul ce contenu final est consommé par Mina. Le flag est activé uniquement dans `.env` local après cette sonde ; `.env.example` conserve le défaut désactivé. Cette preuve ne couvre pas une webcam ou une caméra téléphone physique.
- [x] Fallback OCR local : Tesseract est composé après un échec ou une réponse vide de la vision, sans réseau ni téléchargement. Le test réel d’une image synthétique a retourné `MINA OCR TEST` via `tesseract:eng` ; seules les langues locales `eng` et `osd` sont présentes, pas `fra`.
- [x] Packaging voix locale : la décision eSpeak/Kokoro est fermée pour les artefacts actuellement publiés. `espeak-ng@1.0.2` est confirmé `GPL-3.0-or-later` et `kokoro-js@1.2.1` `Apache-2.0` via les `package.json` installés ; `LICENCES.fr.md` documente que le dépôt public et l’APK Android ne distribuent pas `node_modules`, donc ne déclenchent pas l’obligation GPL à ce stade. Un futur installeur Electron bundlé reste explicitement gaté par `electron_installer_gpl_source_obligation_open`. Le catalogue runtime ne publie plus `espeak_distribution_decision_required` : il rapporte `packaging.local_voice` en `degraded` avec ce gate précis. Preuve ciblée : `npx vitest run tests/local-voice-packaging-policy.test.mjs tests/main-domain-composition-contract.test.mjs --maxWorkers=1 --no-file-parallelism` a passé `2/2` fichiers et `8/8` tests après rouge sur module absent.
- [x] Durcissement FCM Android : le service n’accepte qu’un signal opaque strict (`type`, `ownerId`, `deviceId`, `highWatermark`), ciblé par claims Firebase et planifié par WorkManager unique. Auto-init Messaging et Analytics sont désactivés. Le commit `071d279` a passé le lint/APK, le test Kotlin ciblé (`3/3`) et le contrat Node (`1/1`). Cette preuve ne vaut ni enregistrement FCM/FID live, ni relais cloud propriétaire prêt.
- [x] Flux de réponse chiffré : la vérification Node a exécuté `8` fichiers / `77` tests verts pour le contrat, le ledger, les transports direct/relais et les adaptateurs de streaming. La gate Android ciblée s’est terminée avec `BUILD SUCCESSFUL` en `2m 24s` (`159` tâches : `9` exécutées, `150` à jour), y compris la compilation de l’APK de test Compose. Elle ne constitue ni une exécution instrumentée sur téléphone ni une preuve de stream Firebase cloud.
- [x] Politique de risque domotique locale : le registre applique une borne minimale par `deviceClass` (`light` faible ; prises/interrupteurs/TV/stores/ventilateurs/thermostats moyen ; serrure, garage, portail, alarme, caméra, cuisson, chauffage puissant et vannes bloqués). Une classe inconnue est bloquée et une scène hérite du risque maximal de ses membres ; une scène vide, cyclique ou non résolue est bloquée. `npx vitest run tests/smart-home.test.mjs --maxWorkers=1 --no-file-parallelism` a exécuté `14/14` tests verts. Cela ne configure pas le SDK Google Home, Home Assistant, MQTT ni une commande réelle.
- [x] APK debug Android : l’APK courant `C:\Serveurs\Mina Vision\android\app\build\dist\Mina Vision.apk` mesure `54 245 974` octets, SHA-256 `177AA374550A08FF394AE8920DEAC11969C94ADE0A47551BA4DD825EB83AFA32`, version `0.1.0`. Il a été réinstallé sur le Huawei USB `HUAWEITESTSERIAL` (`lastUpdateTime=2026-08-20 10:23:39`) et le Samsung A71 ADB Wi-Fi `192.168.1.11:44703` (`lastUpdateTime=2026-08-20 10:24:06`), sous le package distinct `fr.mina.gateway.debug`. Les tests instrumentés ont réinitialisé le package debug avant cette réinstallation (`firstInstallTime` revenu au `2026-08-20 10:23/10:24`) : les données locales debug ne doivent pas être considérées préservées. Cette installation ne constitue pas un parcours Telegram/SMS provisionné.
- [x] Identité Windows Electron : les fenêtres Electron utilisent déjà `assets\Logo\mina-vision-256.png`, `app.setName('Mina Vision')` et `app.setAppUserModelId('fr.sourireconcept.minavision')`. Le launcher prépare aussi `node_modules\electron\dist\Mina Vision.exe` avec `assets\Logo\mina-vision.ico`; le stamp local confirme `appUserModelId:"fr.sourireconcept.minavision"`. Le chemin normal a été corrigé : `npm start`, `npm run smoke` et `scripts\start-mina.ps1` passent maintenant par `scripts\launch-mina.ps1`, au lieu de relancer `electron .`. Le connecteur Google ne lance plus `electron` brut non plus : `npm run connect:google` passe par `scripts/run-mina-electron-script.mjs` et donc par le runtime Mina préparé avant de s'arrêter, comme prévu, sur `client_secret_*.json` manquant. Test ciblé : `tests/electron-runtime-icon.test.mjs` `5/5` vert ; `node scripts/prepare-electron-runtime.mjs` retourne `{"ok":true,"exe":"...\\Mina Vision.exe","cached":true}` ; `npm run test:smoke` via launcher termine avec `SMOKE OK`.
- [x] Indexation initiale Mina Code : corpus réel de `935` fichiers indexé en `23 481 ms`, sous l'objectif historique `<30 s`.
- [x] Avatar VRM : explicitement hors périmètre ; aucun asset, modèle, dépendance ou distribution VRM ne doit être ajouté.
- [x] Parsing documentaire local partiel : le runtime compose maintenant la quarantaine avec les parseurs PDF à texte natif et image PNG/JPEG via Tesseract, puis conserve les métadonnées de preuve et les propositions de classification dans des stores SQLite locaux. Le panneau Mina accepte un chemin explicitement saisi, lance quarantaine → extraction locale → proposition de classement après un clic, puis offre un choix de catégorie et une confirmation explicite. Celle-ci n’affiche que des métadonnées redacted, recalcule la conservation par défaut lors d’un changement de catégorie et ne déplace ni ne partage le fichier. Un bouton distinct charge la liste locale de quarantaine : elle affiche seulement nom, type, taille, statut, raisons et date, jamais digest, octets ni texte extrait. Les `16` tests documents/UI ciblés ont passé (`116` tests), le smoke Electron isolé a rendu `SMOKE OK`, l’unitaire de `npm test` a atteint l’intégration via son `&&`, et l’intégration rejouée a passé (`18/18` fichiers, `49/49` tests). Le récapitulatif final de l’unitaire n’a pas été restitué par le superviseur : aucun nouveau total global unitaire n’est affirmé ici. Cette preuve ne couvre aucun document utilisateur, PDF scanné utilisateur ni impression.
  La gate plus récente ci-dessus fournit désormais le total global ; elle ne transforme toujours pas cette preuve en recette sur un document utilisateur, un PDF scanné utilisateur ou une impression.
- [x] Contrôles de runtime sans effet externe : `npm run verify` exécuté le `2026-08-20 14:20` a terminé avec le code `0`. La sonde rapporte `cloudKeys.ready:true`, `androidTransport.ready:true` avec `usb` et `lan`, `wifi.ready:true`, `googleHomeSdk.ready:true` via le SDK/sample détecté sous `C:\Users\Nasro\.mina\sdk\google-home\1.9.1\tools\google-home-api-knowledge-base.txt`, et `windowsSandbox.ready:true` avec `feature/executable/virtualization/ntfs/runtimes` tous verts. Elle rapporte encore `lmStudio.ready:false` / `lm_studio_unreachable` parce que LM Studio est éteint, `mailAccounts.ready:false` / `google_oauth_client_config_missing`, `home.ready:false` / `aucun_connecteur_configure` avec `homeAssistantBaseUrlConfigured:false`, `homeAssistantAuthConfigured:false`, `mqttBrokerConfigured:false`, et `firebase.ready:false` avec `configured:true`, `optional:true`, `firebase_cloud_unverified`. Ce diagnostic ne fait aucune écriture cloud.
- [x] Windows Sandbox réel : les runtimes Sandbox locaux ont été provisionnés dans `C:\Users\Nasro\AppData\Roaming\Mina Vision\cache\sandbox-runtime` après vérification des sources officielles : Python `3.12.7` embeddable SHA256 `0d57bb6cb078b74d23dbfe91f77d6780d45bed328911609f1f7ee2ba1606bf44` confirmé depuis le SBOM SPDX officiel Python, Node `22.23.2` vérifié contre `SHASUMS256.txt`, PowerShell `7.4.19` vérifié contre `hashes.sha256`. Le provisioning installe aussi le runner invité `mina-runner.mjs` et le bootstrap fixe `javascript\node.exe`, désormais exigés par `createRuntimeManifest.verify()`. Recette réelle finale : `npm run verify:release` a lancé `sandbox_smoke`, donc une Windows Sandbox jetable avec `network:false`, produit l’artifact `MINA_SANDBOX_OK v22.23.2`, `receiptExitCode:0`, `eventLogDigest:"sha256:4b78eb5fe88e08659eed215d30c07542b4c73456424c7cda8ee3fb0c27fd1529"`, puis a nettoyé le workspace de job. Tests ciblés : `7` fichiers / `43` tests verts.
- [x] Réinitialisation mémoire locale : après accord de Nasro, `scripts/reinitialize-memory-vault.mjs --probe` via Electron a confirmé un coffre sain avec `secretCount:1`. `scripts/reinitialize-memory-vault.mjs --yes` a archivé uniquement l’état local de Mina dans `C:\Users\Nasro\AppData\Roaming\Mina Vision` : `mina-keyring.json.archive-reset-20260820110734`, `mina-memory.sqlite.archive-reset-20260820110734` et `chat-pc-identity.json.archive-reset-20260820110734`, puis a recréé un coffre sain (`secretCount:0`). L’ancien secret archivé était `google/oauth/client-config`; aucune valeur de secret n’a été affichée. La nouvelle phrase de récupération est écrite localement dans `C:\Users\Nasro\Documents\Mina Vision\PHRASE-RECUPERATION-MINA-VISION-20260820110735.txt` et n’a pas été copiée dans ce rapport.
- [x] Câblage runtime Google : `src/ui/main.mjs` compose déjà `createGoogleRuntimeAdapters`, les adaptateurs Gmail, le hub personnel Google et les services Calendar/Contacts/Tasks quand un compte OAuth est disponible. Les messages opératoires périmés qui annonçaient encore un câblage “non encore fait” ont été corrigés. Preuve ciblée : `npx vitest run tests/live-productivity-tools.test.mjs tests/personal-main-runtime-contract.test.mjs tests/google-runtime-adapters.test.mjs --maxWorkers=1 --no-file-parallelism` a passé `3/3` fichiers et `10/10` tests.
- [x] Durcissement OAuth Google local : le connecteur ne persiste plus un nouveau `clientId/clientSecret` dans le coffre avant succès OAuth. Le test ajouté a d’abord échoué car `keyring.setSecret('google/oauth/client-config', ...)` était appelé après un refus Google `access_denied`, puis il passe après correction. Preuve ciblée : `npx vitest run tests/google-account-connector.test.mjs --maxWorkers=1 --no-file-parallelism` a passé `11/11` tests ; la suite impactée `npx vitest run tests/google-account-connector.test.mjs tests/google-client-config-file.test.mjs tests/google-runtime-adapters.test.mjs tests/live-productivity-tools.test.mjs tests/personal-main-runtime-contract.test.mjs tests/verify-mina-probes.test.mjs --maxWorkers=1 --no-file-parallelism` a passé `6/6` fichiers et `45/45` tests. Avant push, `npm test` a aussi passé l’unitaire (`443/443` fichiers, `3569` passés, `6` ignorés) puis l’intégration (`18/18` fichiers, `49/49` tests).
- [x] Claim ID des résultats de mission : `createMinaRuntime.runWork` attache maintenant un `resultClaimId` aux états de mission `completed` qui contiennent un texte `result`, puis enregistre le claim dans le ledger avec `claimType:"mission_result"` et `status:"verified"`. Le test ajouté a d’abord échoué car le résultat ne contenait pas `resultClaimId`, puis il passe après correction. Preuve ciblée : `npx vitest run tests/mina-runtime.test.mjs --maxWorkers=1 --no-file-parallelism` a passé `7/7` tests ; l’impact grounding/runtime `npx vitest run tests/mina-runtime.test.mjs tests/mina-runtime-v3.test.mjs tests/orchestrator.test.mjs tests/grounding-pipeline.test.mjs tests/claim-ledger.test.mjs tests/response-gate.test.mjs tests/preload-api.test.mjs tests/security-invariants.test.mjs --maxWorkers=1 --no-file-parallelism` a passé `8/8` fichiers et `59/59` tests. Avant push, `npm run verify` a terminé avec le code `0`, `npm test` a passé l’unitaire (`443/443` fichiers, `3570` passés, `6` ignorés) puis l’intégration (`18/18` fichiers, `49/49` tests), et `npm run test:smoke` a rendu `SMOKE OK`.
- [x] Blobs chiffrés pour pièces jointes mail : la migration SQLite `002-mail-attachment-blobs.sql` ajoute `mail_attachment_blobs`, le repository scelle les bytes avec `sealRecord` type `mail_attachment_blob` lié au digest, et le sync service persiste les bytes une seule fois après quarantaine. Le test prouve que le ciphertext SQLite ne contient ni bytes bruts ni base64 en clair et que `getAttachmentBytes(digest)` restaure le buffer attendu. `downloadAttachment` reste absent des providers. Preuve ciblée : `npx vitest run tests/mail-sync-service.test.mjs tests/gmail-adapter.test.mjs tests/microsoft-graph-adapter.test.mjs tests/imap-smtp-adapter.test.mjs tests/google-runtime-adapters.test.mjs --maxWorkers=1 --no-file-parallelism` a passé `5/5` fichiers et `92/92` tests.
- [x] Export local one-shot des pièces jointes mail : le contrôleur mail exporte une pièce jointe par digest `sha256:<64 hex>` uniquement après confirmation locale et sélection d’un chemin par boîte système côté main. Le renderer ne fournit jamais de chemin arbitraire ; il ne transmet que le digest via le canal nommé `mina:mail:export-attachment`. L’écriture passe par `hostWritePolicy.authorize`, fichier temporaire `wx`, puis rename atomique. Le panneau mail affiche un bouton `Exporter …` uniquement pour les pièces jointes dont le digest est valide. Preuve ciblée : `npx vitest run tests/mail-sync-service.test.mjs tests/mail-ipc.test.mjs tests/preload-api.test.mjs tests/domain-panels.test.mjs tests/main-domain-composition-contract.test.mjs tests/live-productivity-tools.test.mjs tests/personal-main-runtime-contract.test.mjs --maxWorkers=1 --no-file-parallelism` a passé `7/7` fichiers et `32/32` tests.

## Reste à faire — décision ou prérequis nécessaire

### Runtime local

- [ ] **Vision Mina par webcam PC et caméra téléphone** — les deux captures sont présentes dans le code (`getUserMedia` côté PC, flux Android signé côté téléphone) et l’adaptateur vision a désormais une preuve image locale. Il reste à tester les permissions, la capture matérielle et le trajet complet sur webcam PC puis téléphone Android autorisé. Ces recettes n’ont pas été exécutées.
- [ ] **Voix locale complète hors réseau** — Kokoro TTS, Gemma streamé et Whisper q8 local sont prouvés séparément, et le STT est activé avec `MINA_OFFLINE=true`. Il reste le trajet matériel microphone → STT → Gemma → Kokoro, réseau désactivé, avec une vraie phrase française. Le test de silence ne permet pas d'affirmer la qualité de transcription.

### Exécution autorisée, encore à réconcilier et terminer

- [ ] **Grounding live** — le gate est maintenant exécuté sur les réponses modèles Telegram et `mina_app` : l’enveloppe JSON est bornée et redacted, les `claimId` sont générés par le ledger local, les segments factuels passent `EvidenceValidator`/`ResponseGate`, et une sortie brute ou non prouvée devient la réponse sûre (`e38856d`, `c05e042`, `59e04fd`). Le flux `mina_app` ne publie plus de chunk avant ce gate ; le runtime démarre avant le canal appairé. Après une action non vérifiée, `done` arrête aussi la mission ; le texte final non vérifié du modèle est remplacé par un résumé déterministe d’actions vérifiées (`547d5c1`, `19bdc84`). Le runtime attache désormais `resultClaimId` au texte de résultat d’une mission complétée et inscrit ce texte dans le claim ledger local. Ces preuves sont locales. Aucun compte Telegram de test, appareil appairé de test ou citation live n’a été exécuté.
- [ ] **Chat natif Android, option A** — Nasro a choisi le chat complet des tâches 13–25. La notification Android n’expose plus le texte de réponse (`2185166`), les règles owner/device sont prouvées localement (`335d8e9`) et le réveil FCM est fail-closed (`071d279`). L’historique Android charge maintenant explicitement des pages de `50` avec curseur tuple et garde au plus `200` objets déchiffrés dans l’état UI ; un message texte sortant en échec final peut aussi être remis dans sa même outbox avec son même `eventId`, sans doublon local. Le `2026-08-20`, `:feature:chat:connectedDebugAndroidTest` a d’abord échoué sur Huawei parce que le champ brouillon existait mais n’était pas affiché sur écran étroit ; le composer a été rendu responsive en séparant contrôles média/voix et champ texte/envoi. Après correction, `:feature:chat:testDebugUnitTest :feature:chat:lintDebug` a terminé `BUILD SUCCESSFUL` en `1m 40s`, `:feature:chat:connectedDebugAndroidTest` ciblé Huawei a terminé `BUILD SUCCESSFUL` avec `9/9` tests, et `testDebugUnitTest lintDebug assembleDebug` a terminé `BUILD SUCCESSFUL` en `1m 41s`. `:app:connectedDebugAndroidTest` a produit des XML `5/5` verts sur Huawei et Samsung pour `RoomMessagingSecretStoreTest`, mais le run Gradle groupé a aussi signalé `Failed to receive the UTP test results` côté Samsung. `:feature:chat:connectedDebugAndroidTest` ciblé Samsung reste rouge `9/9` avec `No compose hierarchies found`; `dumpsys window` prouve que l’appareil est verrouillé/écran non entièrement disponible (`mDreamingLockscreen=true`) avec `NotificationShade` au focus et une autre app au-dessus, donc cette recette Samsung nécessite un déverrouillage manuel stable avant d’être comptée. Le retry média payload v2 couvre maintenant le groupe méta+chunks échoué sans changer les `eventId`; le stop RTDB ignore maintenant les frames déjà en vol après fermeture du listener. Le streaming final reste à réconcilier contre un relais/appareil live. La migration notes/PTT PCM chiffrée reste partielle et la tâche 21 de voix live n’est pas commencée. Il reste la migration du relais Firebase anonyme vers une identité propriétaire/appareil avec custom claims, App Check, Functions et enregistrement FID/FCM explicite ; tant qu’elle n’existe pas, le réveil FCM est volontairement ignoré et aucune tâche Android 13–25 ne peut être déclarée complète.
  - 2026-08-20 15:30 — Recette physique rejouée par Codex sur Huawei USB `HUAWEITESTSERIAL` : les permissions caméra/micro/notifications de `fr.mina.gateway.debug` sont accordées ; la permission SMS a été refusée volontairement puis `REFUSER ET NE PLUS DEMANDER` a été choisi pour éviter d’accorder un droit d’envoi/lecture SMS sans validation explicite. `am start -n fr.mina.gateway.debug/fr.mina.gateway.MainActivity` place ensuite Mina au focus avec PID `10126`. `ANDROID_SERIAL=HUAWEITESTSERIAL .\gradlew.bat :feature:chat:connectedDebugAndroidTest --no-daemon` a terminé `BUILD SUCCESSFUL` en `2m 13s`, `9/9` tests passés sur `MAR-LX1A - 10`. Samsung Wi-Fi `192.168.1.11:44703` reste non compté : deux tentatives ADB avec `KEYCODE_WAKEUP`, `wm dismiss-keyguard`, swipe haut et code `512210` ont laissé `mDreamingLockscreen=true` et `NotificationShade` au focus ; `screencap` a produit un fichier `0` octet dans cet état.
  - 2026-08-20 16:45 — Retry Android média groupé : le test ajouté `retry d une note vocale remet la meta et tous les chunks echoues en file` a d’abord échoué avec une seule ligne remise en outbox, puis passe après requeue atomique du groupe `mediaId` échoué. Preuves : `.\gradlew.bat :core:chat:testDebugUnitTest --tests "fr.mina.gateway.chat.ChatRepositoryTest.retry d une note vocale remet la meta et tous les chunks echoues en file" --no-daemon` a terminé `BUILD SUCCESSFUL`; `.\gradlew.bat :core:chat:testDebugUnitTest --no-daemon` a terminé `BUILD SUCCESSFUL`.
  - 2026-08-20 17:05 — Stop/cancel RTDB Android : le test ajouté `ignore une frame deja en vol apres stop du flux` a d’abord échoué car une callback RTDB conservée pouvait encore livrer une frame après `stop(responseId)`, puis passe après vérification de l’abonnement actif avant `onEvent`. Preuves : `.\gradlew.bat :core:chat:testDebugUnitTest --tests "fr.mina.gateway.chat.FirebaseResponseStreamTest.ignore une frame deja en vol apres stop du flux" --no-daemon` a terminé `BUILD SUCCESSFUL`; `.\gradlew.bat :core:chat:testDebugUnitTest --no-daemon` a terminé `BUILD SUCCESSFUL`.
- [ ] **Téléchargement de pièces jointes mail** — l’ingestion bornée est implémentée pour Gmail et Microsoft Graph (`dd23516`) : seulement les descripteurs `fileAttachment` Graph sont lus, les références ne sont pas suivies, les tailles et encodages sont validés avant la quarantaine. Les bytes de pièces jointes sont persistés localement en blob chiffré, dédupliqué par digest et exportables localement en one-shot par chemin choisi côté main. L’export local est maintenant durci : le renderer transmet `messageId + digest`, le contrôleur refuse une pièce jointe non liée au message ou non `inspectable`, le repository recalcule `sha256:<hex>` avant persistance et après déchiffrement, et l’écriture finale utilise le chemin autorisé avec `flag:'wx'` et `mode:0o600` sans renommage qui pourrait écraser une destination existante. Preuve ciblée du `2026-08-20 17:13` : `npx vitest run tests/attachment-quarantine.test.mjs tests/mail-repository.test.mjs tests/mail-sync-service.test.mjs tests/gmail-adapter.test.mjs tests/microsoft-graph-adapter.test.mjs tests/imap-smtp-adapter.test.mjs tests/mail-ipc.test.mjs tests/mail-service.test.mjs tests/domain-panels.test.mjs tests/preload-api.test.mjs tests/main-domain-composition-contract.test.mjs tests/exclusive-file-writer.test.mjs --maxWorkers=1 --no-file-parallelism` a passé `12/12` fichiers et `144/144` tests après rouges sur export bloqué accepté, absence de lien message↔digest, digest blob non vérifié et writer absent. Il reste une recette réelle avec comptes de test OAuth/Microsoft dédiés. `downloadAttachment` ne doit toujours pas être annoncé par les providers.
- [ ] **Documents / impression** — `main.mjs` compose réellement la quarantaine/intake, le parsing PDF à texte natif et PNG/JPEG via Tesseract, les preuves et la proposition/confirmation de classification dans des stores SQLite locaux, ainsi que l’impression Windows avec confirmation locale. Le panneau Mina permet maintenant, après clic, d’intake/analyser un chemin local explicitement saisi, de générer une proposition, de choisir une catégorie et de la confirmer explicitement. Si la catégorie change, la conservation par défaut est recalculée ; aucune promotion, déplacement ni partage de fichier n’en résulte. Un bouton de lecture distinct affiche la quarantaine locale sous forme de métadonnées limitées (nom, type, taille, statut, raisons, date), sans digest, octets ni texte extrait. La réponse IPC de parsing reste redacted ; une nouvelle vue de preuves affiche seulement index, parseur, page/zone et confiance. Les tests injectent texte, digest et champ privé : aucune de ces données n'atteint le renderer. L’UI permet maintenant de corriger explicitement projet, personne, date, sensibilité et durée de conservation avant confirmation ; le workflow transmet uniquement les champs non vides en overrides, et garde la conservation proposée en placeholder pour ne pas casser le recalcul automatique quand seule la catégorie change. Preuve ciblée : `npx vitest run tests/document-analysis-workflow.test.mjs tests/document-analysis-ui-contract.test.mjs --maxWorkers=1 --no-file-parallelism` a d’abord échoué sur `{ category }` seul et sur les champs HTML absents, puis a passé `2/2` fichiers et `8/8` tests. Les nouvelles écritures de `mina-document-evidence.sqlite` sont désormais `metadata-only` : elles gardent uniquement identifiants techniques, MIME, pagination, parseur, confiance et localisateur validé, jamais texte PDF/OCR, `fields`, `tables`, digest ni propriété privée. Les anciennes lignes de preuves ne sont ni réécrites ni purgées automatiquement. Aucun chiffrement applicatif au repos n’est démontré pour les lignes historiques de preuves ni pour les anciens fichiers sources bruts déjà présents. La quarantaine et le repository JSON exposent maintenant une suppression ciblée testée (`deleteBytes`, `deleteRecord`, `repository.delete`) ; `forgetDocument({ documentId, deleteSource:true })` supprime les chunks RAG injectés et le fichier/record source injectés, avec `sourceFileDeleted:true`. Preuve ciblée : `npx vitest run tests/document-repository.test.mjs tests/document-quarantine.test.mjs tests/document-memory-service.test.mjs --maxWorkers=1 --no-file-parallelism` a passé `3/3` fichiers et `27/27` tests après avoir échoué en rouge sur les méthodes absentes / option ignorée. Ce mécanisme est maintenant composé dans `main.mjs` : le service mémoire document utilise `createDocumentRagRepository` sur `mina-document-rag.sqlite`, avec une clé RAG dérivée du coffre local (`document-rag`) au moment de l’indexation. Le texte sélectionné est stocké dans un payload AEAD chiffré ; les métadonnées nécessaires au comptage/suppression par `documentId` restent en clair. Si le coffre est verrouillé, l’indexation échoue fermée au lieu de persister du texte en clair. Preuve ciblée : `npx vitest run tests/document-rag-repository.test.mjs tests/document-memory-service.test.mjs tests/document-ui-contract.test.mjs tests/main-domain-composition-contract.test.mjs tests/preload-api.test.mjs --maxWorkers=1 --no-file-parallelism` a passé `5/5` fichiers et `44/44` tests après rouges sur payload non chiffré, clé absente et garde RAG incomplète ; `npm run test:smoke` a ensuite retourné `SMOKE OK`. La liste de quarantaine expose maintenant une action danger `Oublier + supprimer source` uniquement quand `documentId` est disponible ; elle demande une confirmation locale explicite, appelle `mina:documents:forget` via preload avec `{ documentId, deleteSource:true }`, puis affiche un accusé `Source supprimée`. Preuve ciblée : `npx vitest run tests/preload-api.test.mjs tests/document-quarantine-list-workflow.test.mjs tests/ui-polish-contract.test.mjs tests/document-analysis-ui-contract.test.mjs --maxWorkers=1 --no-file-parallelism` a passé `4/4` fichiers et `13/13` tests après rouges sur API preload absente, bouton absent et style `.quarantine-actions` absent. La purge par durée de conservation est maintenant automatisée au démarrage : seules les classifications confirmées dont `date`/`confirmedAt` + rétention ISO (`P1Y`, `P10Y`, etc.) sont expirées déclenchent `forgetDocument({ deleteSource:true })`, la suppression de preuve locale et la suppression de classification ; les documents non expirés restent intacts. Preuve ciblée : `npx vitest run tests/document-memory-service.test.mjs tests/main-domain-composition-contract.test.mjs --maxWorkers=1 --no-file-parallelism` a d’abord échoué sur `memory.purgeExpiredDocuments is not a function` puis sur l’appel absent dans `main.mjs`, puis a passé `2/2` fichiers et `18/18` tests ; la suite documents/composition `npx vitest run tests/document-memory-service.test.mjs tests/document-evidence-store.test.mjs tests/document-classifier.test.mjs tests/document-repository.test.mjs tests/document-quarantine.test.mjs tests/document-rag-repository.test.mjs tests/main-domain-composition-contract.test.mjs --maxWorkers=1 --no-file-parallelism` a passé `7/7` fichiers et `58/58` tests. Les nouvelles écritures de fichiers sources de quarantaine sont maintenant chiffrées avec une enveloppe AEAD locale et une clé HKDF dédiée `document-quarantine`; si le coffre ne fournit pas de clé, l’écriture échoue fermée au lieu d’écrire en clair. La promotion résout désormais le dossier parent réel d’un nouveau fichier, autorise le chemin canonique et conserve l’écriture `wx` ; elle refuse les chemins relatifs, UNC et toute autorisation qui redirige le fichier. Un PDF sans texte extrait bascule désormais vers le rendu local borné (10 pages, 150 DPI, 3 000 000 pixels/page, 2 MiB PNG/page, 10 MiB cumulés) puis Tesseract, sans fichier temporaire ni réseau. Une fixture PDF synthétique uniquement image a produit `MINA OCR` avec une preuve `ocr` page 1 et une confiance `0,95105408` ; après persistance réelle, le texte reconnu était absent et chaque bloc ne contenait que `sourceOffset` et `confidence`. Seul `eng` était installé, donc la qualité française reste non vérifiée. Le signal d’annulation est relayé du parseur à l’import PDF.js, au chargement (`destroy()`), à `getPage`, au rendu (`cancel()`), à l’extraction texte et au processus Tesseract ; les buffers accessibles sont effacés sans masquer un buffer PDF.js/Tesseract détaché. Une sonde locale sans donnée utilisateur a annulé Tesseract réel en `16 ms`. Le panneau expose maintenant `Annuler l’analyse` pendant la phase de parsing ; l’IPC ne transmet que le `documentId`, le contrôleur relaie le signal et une annulation acceptée empêche `evidenceStore.store`. Le bouton disparaît dès le retour de `parse`, avant la lecture des preuves et le classement, afin de ne jamais annoncer une annulation pendant une persistance. Une annulation ne supprime pas l’original déjà placé en quarantaine. Formulaires, conversion et téléchargement restent non composés et échouent explicitement avec `*_not_configured`, jamais par une valeur `undefined`. Le contrôleur urgence est enregistré uniquement lorsque le corpus et le mode réels sont composés ; le renderer expose encore seulement son état, sans action de construction ou d’activation. Aucune promotion via UI, analyse d’un document utilisateur, réception physique d’impression ou état terminal spooler vérifié n’a été exécuté.
  - 2026-08-20 15:55 — Réconciliation chiffrement quarantaine : les nouvelles écritures de fichiers sources passent maintenant par une enveloppe AEAD locale avec AAD `document_quarantine_source` et clé HKDF dédiée `document-quarantine` dérivée du coffre. Si la clé est indisponible, l’écriture échoue fermée. Les anciens fichiers sources bruts restent lisibles pour rétrocompatibilité ; ils ne sont pas migrés automatiquement. Preuve ciblée : `npx vitest run tests/document-quarantine.test.mjs tests/main-domain-composition-contract.test.mjs --maxWorkers=1 --no-file-parallelism` a d’abord échoué sur un stockage identique au plaintext et sur le câblage absent, puis a passé `2/2` fichiers et `22/22` tests.
  - 2026-08-20 17:47 — Correction du statut formulaire : la phrase ci-dessus “Formulaires, conversion et téléchargement restent non composés” est désormais partiellement périmée. Les propositions et aperçus de formulaires sont composés via `createFormService` et exposés par preload (`mina:documents:propose-fill`, `mina:documents:render-form-preview`). La copie remplie reste explicitement bloquée par `document_form_rendering_unavailable` faute de renderer PDF réel ; conversion et téléchargement restent non composés. Preuve ciblée : `npx vitest run tests/preload-api.test.mjs tests/main-domain-composition-contract.test.mjs tests/document-ui-contract.test.mjs tests/form-service.test.mjs --maxWorkers=1 --no-file-parallelism` a passé `4/4` fichiers et `45/45` tests après rouges sur API preload absente et composition `main.mjs` absente.
  - 2026-08-20 18:01 — Correction du statut téléchargement : la phrase ci-dessus “conversion et téléchargement restent non composés” est désormais partiellement périmée. Le téléchargement documentaire est composé dans `main.mjs` via `createDownloadService` + `createHttpDocumentDownloadPort`, exposé par preload (`mina:documents:download`) et soumis à confirmation locale par digest avant fetch. Le port refuse les URL non-HTTPS, les adresses locales/privées littérales, les redirections non vérifiées et les réponses trop grandes ; l’écriture finale passe par `hostWritePolicy`, `flag:'wx'` et `mode:0o600`. Conversion et copie de formulaire remplie restent non composées. Preuve ciblée : `npx vitest run tests/download-service.test.mjs tests/document-download-port.test.mjs tests/preload-api.test.mjs tests/main-domain-composition-contract.test.mjs tests/document-ui-contract.test.mjs --maxWorkers=1 --no-file-parallelism` a passé `5/5` fichiers et `41/41` tests après rouges sur API preload absente et composition `main.mjs` absente.

- [x] **Gate de release, push et asset APK** — la branche `master` a été poussée sur `origin/master` après les suites Node et Android listées ci-dessus. La release GitHub `v0.1.0` contient maintenant l’asset `Mina.Vision.apk` à `54 245 974` octets (`https://github.com/Nasro-B/Mina-Vision/releases/download/v0.1.0/Mina.Vision.apk`). L’ancien asset `app-debug.apk` reste présent à `53 960 106` octets ; il n’a pas été supprimé car cette suppression est une action externe destructrice. Les validations manuelles Android/Home/mail/Sandbox/voix micro hors ligne restent séparées.

### Preuves externes ou physiques

- [ ] **Accès Firebase Mina Vision** — Nasro indique que le compte propriétaire Berkoun doit être
  le compte opérateur de `mina-vision` et que le projet Firebase cible est l’URL console `/u/5/project/mina-vision`, pas `/u/0/project/mina-vision`. Cette sélection doit être revalidée en lecture seule avant toute nouvelle écriture cloud. La preuve historique de console/CLI a été effectuée sous `Compte Google Sourire Concept (mina.vision.ai@gmail.com)` dans le projet Mina Vision ; elle ne prouve donc pas que le compte opérateur cible est maintenant celui de Berkoun. La base RTDB est en Belgique (`europe-west1`) avec l’endpoint `https://mina-vision-default-rtdb.europe-west1.firebasedatabase.app`. Nasro a aussi fourni le bucket Storage `gs://mina-vision.firebasestorage.app`, classe `Regional`, emplacement `US-CENTRAL1`, accès `Standard`. La sonde locale du `2026-08-20` confirme seulement `firebase.configured:true` et `firebase_cloud_unverified`; elle ne fait pas d’appel cloud implicite.
- [ ] **Android utilisateur / recettes physiques** — `adb devices -l` liste actuellement le Huawei USB `HUAWEITESTSERIAL` (`MAR_LX1A`) et le Samsung A71 Wi-Fi `192.168.1.11:44703` (`SM_A715F`) ; ADB liste aussi le même Samsung via mDNS (`adb-SAMSUNGTESTSERIAL-a80QcZ._adb-tls-connect._tcp`), donc les commandes ciblent l’IP explicite pour éviter les doublons. L’APK local `android\app\build\outputs\apk\debug\app-debug.apk` et l’asset local `android\app\build\dist\Mina Vision.apk` ont le même SHA256 `177AA374550A08FF394AE8920DEAC11969C94ADE0A47551BA4DD825EB83AFA32`, la même taille `54 245 974` octets et correspondent au binaire attaché à la release `v0.1.0`. Le `2026-08-20`, `adb -s HUAWEITESTSERIAL install -r android\app\build\outputs\apk\debug\app-debug.apk` a retourné `Success`, puis `dumpsys package fr.mina.gateway.debug` a confirmé `versionName=0.1.0`, `versionCode=1`, `lastUpdateTime=2026-08-20 14:54:50`. Sur Samsung, `adb install -r` via streaming Wi-Fi est resté bloqué et n’a pas été compté ; le chemin fiable `adb push ... /data/local/tmp/mina-debug.apk` (`54 245 974` octets poussés) puis `adb shell pm install -r /data/local/tmp/mina-debug.apk` a retourné `Success`, le fichier temporaire a été supprimé, et `dumpsys package fr.mina.gateway.debug` a confirmé `lastUpdateTime=2026-08-20 14:57:34`. `pm list packages` confirme maintenant `fr.mina.gateway.debug` sur Huawei, et `fr.mina.gateway` + `fr.mina.gateway.debug` sur Samsung : ce sont les variantes release/debug avec des package IDs distincts, pas une duplication Android du même APK. `cmd package resolve-activity --brief fr.mina.gateway.debug` retourne `fr.mina.gateway.debug/fr.mina.gateway.MainActivity` sur les deux. Au `2026-08-20 15:30`, Huawei a reçu les permissions caméra, micro et notifications pour `fr.mina.gateway.debug`; la permission SMS a été refusée puis marquée `REFUSER ET NE PLUS DEMANDER` pour ne pas accorder un droit d’envoi/lecture SMS sans validation explicite. `am start` place maintenant `fr.mina.gateway.debug/fr.mina.gateway.MainActivity` au focus avec PID `10126`, et `ANDROID_SERIAL=HUAWEITESTSERIAL .\gradlew.bat :feature:chat:connectedDebugAndroidTest --no-daemon` a passé `9/9` tests (`BUILD SUCCESSFUL`). Samsung reste non compté : les tentatives de déverrouillage ADB avec le code fourni ont laissé `mDreamingLockscreen=true` et `NotificationShade` au focus. Aucun parcours complet SMS, Telegram, caméra téléphone utilisateur, micro utilisateur ou chat utilisateur réel n’est encore validé.
- [ ] **Google Home / Home Assistant** — le runtime compose la politique de risque domotique locale, et la sonde reconnaît maintenant le SDK/sample Google Home réellement présent en `C:\Users\Nasro\.mina\sdk\google-home\1.9.1` grâce au fichier `tools\google-home-api-knowledge-base.txt` annoncé par son README. Preuve ciblée : `npx vitest run tests/verify-mina-probes.test.mjs --maxWorkers=1 --no-file-parallelism` a passé `16/16` tests, et `npm run verify` retourne `googleHomeSdk.ready:true`. Le domaine `home` reste `home.ready:false`, `reason:"aucun_connecteur_configure"` tant que `HOME_ASSISTANT_BASE_URL` + `HOME_ASSISTANT_TOKEN` n’existent pas. Le code ne crée plus un registre vide quand Home Assistant est configuré : `main.mjs` découvre les entités du connecteur, les convertit en appareils Mina (`light`, `switch`, `cover`, `climate`, `scene`) et marque le domaine dégradé avec `home_no_device_discovered` si aucun appareil n’est trouvé. Le connecteur Home Assistant accepte maintenant aussi `read_state` pour les domaines mappés. Preuve ciblée : `npx vitest run tests/home-assistant-discovery.test.mjs tests/home-device-discovery.test.mjs tests/home-assistant-adapter.test.mjs tests/compose-home-domain.test.mjs tests/home-ipc.test.mjs tests/smart-home.test.mjs tests/main-domain-composition-contract.test.mjs --maxWorkers=1 --no-file-parallelism` a passé `7/7` fichiers et `52/52` tests après rouges sur module discovery absent et `read_state` refusé ; `npm run test:smoke` a retourné `SMOKE OK`. MQTT reste explicitement indisponible car sa dépendance runtime a été retirée ; aucun connecteur Google Home de contrôle d’appareils n’est encore branché côté Electron. Il reste donc à fournir une configuration Home Assistant réelle ou à concevoir/implémenter un pont Google Home, puis à exécuter une recette supervisée sur une lumière non critique.
  - 2026-08-20 16:22 — Correctif UI Home : le bouton `Exécuter` ne transmet plus le payload brut `{ deviceId, command }`, incompatible avec `createSmartHomeService.execute`. Le renderer construit maintenant un intent normalisé (`turn_on`, `turn_off`, `read_state`) avec `commandId` UUID v4, `expiresAt` borné à 60 s, `sourceChannel:"local_ui"` et `sessionId:"home-ui"`. Preuve ciblée : `npx vitest run tests/home-flow-dom.test.mjs --maxWorkers=1 --no-file-parallelism` a d’abord échoué sur le payload brut puis a passé `1/1` test. Cela ne configure toujours pas de connecteur réel : `npm run verify` reste `home.ready:false` tant que `HOME_ASSISTANT_BASE_URL` + `HOME_ASSISTANT_TOKEN` sont absents.
- [ ] **Mail fournisseurs / Gmail** — le runtime applicatif est câblé, mais aucun compte n’est enregistré. Avant la réinitialisation mémoire, le diagnostic exposait un client local non sensible rattaché au projet OAuth `mina-vission` alors que `.env` cible `mina-vision`, avec `oauthProjectMatchesFirebase:false`; `connect:google` refusait donc ce mismatch avant OAuth. Après la réinitialisation mémoire locale, l’ancien secret `google/oauth/client-config` a été archivé et `npm run verify` retourne maintenant `mailAccounts.ready:false`, `reason:"google_oauth_client_config_missing"`. Le fichier local `env/client_secret_486101146642-26r5tosh6jopnerpmnjiopjm18orle84.apps.googleusercontent.com.json` a ensuite été déplacé sans suppression vers `env/archive-oauth-mismatch/`, afin que Mina ne reprenne plus automatiquement ce client du mauvais projet. Le scan local des noms de variables `.env` confirme `MINA_GOOGLE_ACCOUNT`, mais aucun fichier `env/client_secret*.json` n’existe. Le `2026-08-20`, `npm run connect:google` ne bloque plus sur `google_account_connector_address_required` : il arrive au prompt `Client ID Google :`, puis a été interrompu volontairement faute de client OAuth Desktop valide. L’erreur Google constatée par Nasro est `Erreur 403 : access_denied`, avec le message indiquant que Mina Vision n’a pas terminé la validation Google et que seuls les testeurs approuvés y ont accès. La lecture Chrome confirme que le projet cible est `mina-vision` sous `authuser=5`, le compte visible est `mina.vision.ai@gmail.com`, et l’écran Google Auth Platform est à l’étape initiale `Créer un branding` avec les champs `Nom de l'application`, `Adresse e-mail d'assistance utilisateur`, audience `Interne/Externe`, e-mails de test et une case légale `J'accepte le Règlement sur les données utilisateur dans les services d'API Google.`. Aucune modification cloud ni acceptation légale n’a été effectuée. Gmail ne peut donc pas devenir opérationnel tant que Google Auth Platform n’est pas configuré dans `mina-vision`, que `mina.vision.ai@gmail.com` n’est pas ajouté comme testeur si l’audience reste en test, qu’un client OAuth Desktop `mina-vision` n’est pas téléchargé dans `env/client_secret_*.json`, puis que `npm run connect:google` n’a pas été rejoué avec succès.
  - 2026-08-20 15:12 — Chrome extension : le formulaire Google Auth Platform a été rempli mais pas soumis. Champs visibles renseignés/sélectionnés : application `Mina Vision`, support `mina.vision.ai@gmail.com`, audience `Externe`, contact développeur `mina.vision.ai@gmail.com`. L’écran final affiche la case non cochée `J'accepte le Règlement sur les données utilisateur dans les services d'API Google.` ; Codex n’a pas coché la case et n’a pas cliqué `Créer`. Aucune configuration OAuth durable, aucun client OAuth Desktop et aucun testeur OAuth ne sont donc confirmés.
  - 2026-08-20 16:12 — Durcissement local : `connect:google` n’accepte plus la saisie manuelle Client ID/Secret quand `.env` cible `mina-vision` et qu’aucun `env/client_secret_*.json` n’est présent. Le script sort avant Chrome avec `Client OAuth Desktop Google requis pour le projet "mina-vision"` afin d’éviter de réutiliser un client OAuth du mauvais projet. Preuve ciblée : `npx vitest run tests/google-account-connector.test.mjs tests/google-client-config-file.test.mjs tests/connect-google-account-script.test.mjs tests/google-runtime-adapters.test.mjs tests/live-productivity-tools.test.mjs tests/personal-main-runtime-contract.test.mjs tests/verify-mina-probes.test.mjs --maxWorkers=1 --no-file-parallelism` a passé `7/7` fichiers et `49/49` tests après rouge sur saisie manuelle encore acceptée ; test live local `MINA_GOOGLE_ACCOUNT=mina.vision.ai@gmail.com npm run connect:google` a retourné code `1` avec le message attendu, sans prompt Client ID.
- [x] **Windows Sandbox** — `npm run verify` rapporte maintenant `windowsSandbox.ready:true` et `sandbox.status:"available"`. La recette `npm run smoke:sandbox` a exécuté un job JavaScript minimal dans une Windows Sandbox jetable avec réseau désactivé, a récupéré un artifact vérifié et a retourné `receiptExitCode:0`. Ce point ne prouve pas encore l'exécution de tests applicatifs lourds dans Sandbox, mais le prérequis runtime/isolation demandé par la release est désormais couvert par une preuve live.
- [x] **Chrome via l'extension demandée** — la session Chrome demandée fonctionne ; elle a relu l’état console sans effectuer de modification cloud. Elle confirme que le blocage OAuth Gmail est externe au runtime Gmail de Mina : l’erreur 403 utilise le client `486101146642-26r5tosh6jopnerpmnjiopjm18orle84.apps.googleusercontent.com`, tandis que la page Google Auth Platform du projet cible `mina-vision` sous `authuser=5` indique que la plateforme d’authentification n’est pas encore configurée.
  - 2026-08-20 15:12 — Le formulaire non soumis dans Chrome est prêt à l’étape finale, mais la création OAuth reste bloquée par validation explicite de Nasro sur le règlement Google API User Data Policy.

## Ordre de reprise proposé

1. Débloquer Gmail côté Google Cloud : configurer Google Auth Platform dans `mina-vision`, choisir l’audience de test adaptée, ajouter `mina.vision.ai@gmail.com` comme testeur si l’app reste en test, créer/télécharger un client OAuth Desktop dans ce même projet, déposer le JSON dans `env/client_secret_*.json`, puis rejouer `npm run connect:google`.
2. Configurer Home : fournir le SDK Google Home signé ou un connecteur Home Assistant/MQTT, puis tester une lumière non critique avec supervision.
3. Relancer LM Studio uniquement pour les recettes modèles/voix/vision, puis rejouer `npm run verify` et le trajet microphone français complet.
4. Sur les deux téléphones, accepter manuellement les permissions nécessaires puis exécuter les recettes caméra, micro, Telegram/SMS et chat natif Android.
5. Réconcilier le code courant avec les plans chat Android, grounding, documents et impression, puis traiter les écarts test par test.
6. Après la prochaine vague de code, rejouer `npm run verify:release` et actualiser ce tableau avec les sorties réellement observées.
